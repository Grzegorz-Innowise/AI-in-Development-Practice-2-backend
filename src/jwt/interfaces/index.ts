/**
 * Exports all JWT-related interfaces for easy import across the application.
 *
 * @module JwtInterfaces
 */

export { type JwtPayload } from './jwt-payload.interface';
export { type JwtToken } from './jwt-token.interface';
